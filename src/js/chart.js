/* ── CHART ───────────────────────────────────────────────────── */
/* Chart.js replaced with Alpine CSS bar chart (chartData store getter).
   Stub exports kept so existing import sites compile without change.   */

export function initChart()  {}
export function updateChart() {}
export function resetChart()  {}
